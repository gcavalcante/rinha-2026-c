Coloque aqui o arquivo oficial `references.json.gz` antes de executar:

```bash
cp /caminho/para/references.json.gz resources/references.json.gz
```

O Dockerfile principal espera esse arquivo para gerar e embarcar `/app/index.bin` durante o build.
